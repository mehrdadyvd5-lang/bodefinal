<UserControl x:Class="BodyComposition.App.Views.Sections.ExerciseSuggestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource GlassCard}">
        <StackPanel>
            <TextBlock Text="{DynamicResource Str_MyExerciseSuggestion}" FontSize="16" FontWeight="Bold"
                       Foreground="{StaticResource BrandGreenDarkBrush}" Margin="0,0,0,12" />
            <TextBlock x:Name="SummaryText" TextWrapping="Wrap" FontSize="13" Foreground="#222" Margin="0,0,0,16" />
            <Button Content="{DynamicResource Str_Questionnaire}" Style="{StaticResource PillButton}"
                    Width="180" HorizontalAlignment="Left" Click="OpenQuestionnaire_Click" />
        </StackPanel>
    </Border>
</UserControl>
