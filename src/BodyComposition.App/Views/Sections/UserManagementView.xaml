<UserControl x:Class="BodyComposition.App.Views.Sections.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource GlassCard}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{DynamicResource Str_UserAdministration}" FontSize="16" FontWeight="Bold"
                       Foreground="{StaticResource BrandGreenDarkBrush}" Margin="0,0,0,10" />

            <TextBox x:Name="SearchBox" Grid.Row="1" Style="{StaticResource GlassTextBox}" Margin="0,0,0,12"
                     TextChanged="SearchBox_TextChanged" />

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="UsersList" />
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
